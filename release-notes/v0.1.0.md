- **画面を一新しました。** ノッチ表示がウィンドウの主役になり、EB・B8〜B1・N・
  P1〜P5 のノッチ列をそのまま並べて、いま入っている位置だけが点灯します。全体を
  一度に見られるので、キャリブレーションのずれもその場で分かります。非常ブレーキ
  をハンドルに含めていない場合は、EB が枠だけの表示で残ります。
- **配色はアイコンと同じ**です。ブレーキ側が赤、N が緑、力行が青。この 3 色だけを
  濃い計器盤の上に置き、ほかは白いカードと灰色の背景に抑えました。
- **軸の一覧が行ごとの表示になりました。** 選択中の軸が強調され、行をクリックする
  だけで軸を選べます。レバーを動かして反応した行を、その場で選べます。
- **縦 5 段だったものを 2 列にしました。** 以前は縦に長すぎて 1080p の 125% 表示に
  収まりませんでしたが、収まるようになりました。
- **日本語に Yu Gothic UI を使うようにしました。** Segoe UI にはかなが無く、Windows
  が補う書体が周りのラテン文字と揃わないためです。
- 設定ファイルのパスが途中で折り返していたのを 1 行にまとめ、全体はマウスを乗せると
  見られるようにしました。
- リリースノートを日本語にし、各版の変更点を載せるようにしました。

動作は変わっていません。ノッチ値・HID レポート・軸の読み取りには手を入れていない
ので、v0.0.3 と同じように動きます。変わったのは見た目だけです。

<details>
<summary>English</summary>

- **The control panel was redrawn.** The notch display is now what the window is
  built around: EB, B8 to B1, N and P1 to P5 laid out as the scale they are, lit one
  at a time. Seeing all of it at once also shows a badly calibrated axis for what it
  is. With EB off the handle it stays on the scale as an empty outline.
- **The palette is the icon's**: red brake, green neutral, blue power, the only
  saturated colour in the window and on its only dark surface. Everything else is
  white cards on a grey page.
- **The axis readings are one row each.** The chosen axis is highlighted, and
  clicking a row picks it — the answer you want at the moment the lever moves.
- **Five stacked sections became two columns.** The old layout had grown too tall
  for a 1080p screen at 125%; it fits now.
- **Japanese uses Yu Gothic UI.** Segoe UI has no kana, and the face Windows links
  in for them does not match the Latin beside it.
- The configuration path was wrapping mid-directory. One line now, with the full
  path on the tooltip.
- The release notes are published in Japanese, with a changelog for each version.

Nothing changed in how the bridge works: the notch values, the HID report and the
axis reading are untouched, so it behaves exactly as v0.0.3 did.

</details>
