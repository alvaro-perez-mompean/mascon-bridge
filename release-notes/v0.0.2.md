- **日本語表示になりました。** 既定が日本語で、ウィンドウ上部のセレクターから
  English に切り替えられます。選んだ言語は `config.json` に保存され、ウィンドウは
  その場で新しい言語に切り替わります。ウィンドウ・ダイアログ・コンソールの表示は
  すべて翻訳済みです。(#6)
- **コントロールパネルから機種を選べるようになりました。** 既定は **ZKNS-002** です。
  ゲームが反応するのは ZUIKI のベンダー ID `33DD` を持つ機種だけで、Nintendo 由来の
  `0F0D` を持つ ZKNS-001 は無視されます。うまく動かないときは、まず機種を確かめて
  ください。(#3)
- **README は日本語を表紙にしました。** 英語版は `README.en.md` にあり、互いに
  リンクしています。どちらも zip に同梱します。(#8)
- **リリースが単一ファイルになりました。** 展開すると 274 個あったファイルが、
  プログラム・README・ライセンス・第三者表記だけになります。(#5)
- **導入手順を整理しました。** リリース版を使う人向けの「インストール」と
  「ソースからのビルド」に分け、起動するのは `mascon-bridge.cmd` ではなく
  `mascon-bridge.exe` であることを明記しました。(#4)
- **アプリケーションアイコンを追加しました。** マスコンを上から見た形を単純化した
  もので、README の先頭にも掲載しています。(#2)
- CI の `GITHUB_TOKEN` の権限を読み取りのみに制限しました（CodeQL の指摘による）。(#1)

<details>
<summary>English</summary>

- **The program now speaks Japanese**, with English available from a selector at
  the top of the window. The choice is kept in `config.json` and the window
  reopens in the new language immediately. Window, dialogs and console modes are
  all translated. (#6)
- **The mascon model can be chosen from the control panel**, and the default is
  now **ZKNS-002**. The game only reacts to models on ZUIKI's `33DD` vendor id;
  ZKNS-001 carries Nintendo's `0F0D` and is ignored. If the game does not react,
  check the model first. (#3)
- **Japanese is now the front page of the README**, with the English one in
  `README.en.md`. Each links to the other, and both ship in the zip. (#8)
- **The release is published as a single file.** Unzipping used to give 274
  files; it now gives the program, the README, the licence and the third party
  notices. (#5)
- **Installing and building are separate sections now**, and the thing to run is
  `mascon-bridge.exe`, not `mascon-bridge.cmd`. (#4)
- **Added an application icon**, a simplified top-down view of the mascon, also
  used as the header of the README. (#2)
- Limited the CI `GITHUB_TOKEN` to read access, from a CodeQL alert. (#1)

</details>
